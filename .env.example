# User and Group ID settings
# Set these to match your host user's UID and GID to ensure proper file permissions
# on mounted volumes. Run `id` on your host system to find your UID and GID.
PUID=1000
PGID=1000
